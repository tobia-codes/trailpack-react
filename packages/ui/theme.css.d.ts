// Type stub for the `@trailpack/react-ui/theme.css` export, for the same reason
// as `styles.css.d.ts` next to it: the stylesheet is imported for its side
// effects only, but TypeScript still needs the subpath to resolve to *something*
// typed, or consumers without ambient `*.css` declarations fail with TS2882.
export {};
