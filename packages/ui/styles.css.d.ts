// Type stub for the `@trailpack/react-ui/styles.css` export. The stylesheet is
// imported for its side effects only, but TypeScript still needs the subpath to
// resolve to *something* typed — without this, consumers that do not pull in
// ambient `*.css` declarations (via `vite/client` or similar) fail with
// TS2882 on `import '@trailpack/react-ui/styles.css'`.
export {};
